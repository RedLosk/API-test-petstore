# 🚀 API Autotests Project

Проект автоматизированного тестирования API с использованием современных инструментов и best practices.

## 🛠 Технологии

| Технология | Версия | Описание |
|-----------|--------|----------|
| **Python** | 3.8+ | Основной язык программирования |
| **Pytest** | latest | Фреймворк для тестирования |
| **Allure Report** | latest | Система отчетности и визуализации результатов |
| **Requests** | latest | HTTP библиотека для API запросов

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/RedLosk/API-test-petstore
cd API-test-petstore
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv .venv
.venv\Scripts\activate
```

**macOS/Linux:**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

## 🚀 Запуск тестов

### Базовый запуск

```bash
pytest
```

### Запуск с подробным выводом

```bash
pytest -v
```

### Запуск конкретного тестового файла

```bash
pytest tests/test_api.py
```

### Запуск конкретного теста

```bash
pytest tests/test_api.py::test_function_name
```

## 📊 Отчеты

### Генерация Allure отчета

1. Запустите тесты с сохранением результатов:

```bash
pytest --alluredir=./allure-results
```

2. Сгенерируйте и откройте отчет:

```bash
allure serve ./allure-results
```

### Генерация HTML отчета

```bash
allure generate ./allure-results -o ./allure-report --clean
```

Откройте `allure-report/index.html` в браузере.


